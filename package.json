{
  "name": "nova-proxy",
  "version": "4.7.4",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "cloudflare": {
    "bindings": {
      "KV": {
        "description": "Nova's compatibility and migration store. Cloudflare creates and binds it automatically."
      },
      "DB": {
        "description": "Nova's primary D1 database for settings, users, usage, logs, and authentication."
      },
      "NOVA_CLAIM_TOKEN": {
        "description": "Optional but recommended. A random string that ties first-time setup to you."
      }
    }
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "esbuild": "^0.28.1",
    "html-minifier-terser": "^7.2.0",
    "wrangler": "^4.116.0"
  }
}
